[project]
name = "plot-loc"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["matplotlib>=3.10.1", "pygit2>=1.17.0"]

[project.scripts]
plot-loc = "plot_loc.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
